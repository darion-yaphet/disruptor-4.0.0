plugins {
    // Auto-provision the Java 11 toolchain (and others) when no matching local JDK is found.
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'disruptor'
